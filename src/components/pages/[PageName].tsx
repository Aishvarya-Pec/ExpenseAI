export const PageName: React.FC = () => {
  // ... existing code ...

  return (
    <div className="relative p-6">
      {/* Remove: ParticleBackground, AnimatedBackground, Sparkles */}
      
      <div className="relative z-10 space-y-6">
        {/* All existing page content stays the same */}
        {/* ... existing content ... */}
      </div>
    </div>
  );
};